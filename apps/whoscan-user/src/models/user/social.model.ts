export interface Social {
    id: string
    name: string;
    prefix_mobile: string;
    prefix: string;
    icon: string;
    active: boolean;
}

export interface Social {
    id: string
    name: string;
    prefix_mobile: string;
    prefix: string;
    icon: string;
    active: boolean;
}